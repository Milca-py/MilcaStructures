implementar:
 - clase results,
 - desing, 
 - analysis, 
 - load case,
 - load combination,


ME QUEDE EN .................
- odenar los resultados : desplazaments and reactions
- para GUID usar parametro de clase en system
- usar MatrixException, excepciones





FALTA IMPLEMENTACIONES PARA: -------------
- peso propio
- active de load pattern
- auto load pattern y case
- pattern type
- element type
- deformaciones por cortadura








- deformada + desplazaments en los nodos
- diagramas individuales
- reacciones en los apoyos
- combianar widgets con plots
- craer un propio ploteador para la libreria













# DEFECTOS ---------------
- se tiene que resolver antes de graficar
- que cada calase almacene datos, en lugar de ello solo sean macros que asignen datos a clases de elementos y nudos y systema




















# subir a gitbub:
cd c:users/amilcar/documents/github/milcastructures
git checkout main
git pull origin main
git add .
git commit -m "Mensaje descriptivo del cambio"
git push origin main  # O la rama en la que trabajas
exit


PARA EL 3D =================================================


clases:

Element:
    self.id = Element.id
    Element.id += 1
    self.start_node = start_node
    self.end_node = end_node
    self.section = section
    self.material = material
    self.length = 
    @property
    def degrees_of_freedom(self):
    @property
    def matrix(self): = A.T @ K @ A



DegreeOfFreedom:
    self.id = DegreeOfFreedom.id
    DegreeOfFreedom.id += 1
    self.__displacement_value = 0
    self.__restrained = False
    self.__force = 0


    @property
    def displacement(self):

    @property
    def force(self):

    @property
    def restrained(self):

    @property
    def displaced(self):



class Node:
    self.id = Node.__node_id
    Node.__node_id += 1
    self._x = x
    self._y = y
    self._z = z
    self.dof_1 = DegreeOfFreedom()
    self.dof_2 = DegreeOfFreedom()
    self.dof_3 = DegreeOfFreedom()
    self.dof_4 = DegreeOfFreedom()
    self.dof_5 = DegreeOfFreedom()
    self.dof_6 = DegreeOfFreedom()
